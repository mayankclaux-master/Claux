/**
 * CLAUX Runtime SDK Layer - Constants
 */

/**
 * Default Session TTL (1 hour)
 */
export const DEFAULT_SESSION_TTL = 3600000;

/**
 * Default Subscription TTL (24 hours)
 */
export const DEFAULT_SUBSCRIPTION_TTL = 86400000;

/**
 * Default Stream Buffer Size
 */
export const DEFAULT_STREAM_BUFFER_SIZE = 1000;
