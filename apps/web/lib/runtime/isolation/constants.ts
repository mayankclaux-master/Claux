/**
 * CLAUX Runtime Isolation Layer - Constants
 */

/**
 * Default CPU Limit
 */
export const DEFAULT_CPU_LIMIT = 4.0;

/**
 * Default Memory Limit (GB)
 */
export const DEFAULT_MEMORY_LIMIT = 16.0;

/**
 * Default Storage Limit (GB)
 */
export const DEFAULT_STORAGE_LIMIT = 100.0;

/**
 * Default Network Limit (Mbps)
 */
export const DEFAULT_NETWORK_LIMIT = 1000;

/**
 * Scheduling Fairness Weight
 */
export const DEFAULT_FAIRNESS_WEIGHT = 1.0;

/**
 * Checkpoint TTL (24 hours)
 */
export const CHECKPOINT_TTL = 86400000;
