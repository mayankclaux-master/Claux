/**
 * CLAUX Runtime Scaling Layer - Constants
 */

/**
 * Default Scale Up Threshold
 */
export const DEFAULT_SCALE_UP_THRESHOLD = 0.8;

/**
 * Default Scale Down Threshold
 */
export const DEFAULT_SCALE_DOWN_THRESHOLD = 0.3;

/**
 * Default Minimum Workers
 */
export const DEFAULT_MIN_WORKERS = 1;

/**
 * Default Maximum Workers
 */
export const DEFAULT_MAX_WORKERS = 16;

/**
 * Default Stabilization Window (5 minutes)
 */
export const DEFAULT_STABILIZATION_WINDOW = 300000;

/**
 * Default Prediction Horizon (10 minutes)
 */
export const DEFAULT_PREDICTION_HORIZON = 600000;
