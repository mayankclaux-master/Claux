/**
 * CLAUX Runtime Persistence Layer - Constants
 */

/**
 * Default Snapshot Interval (5 minutes)
 */
export const DEFAULT_SNAPSHOT_INTERVAL = 300000;

/**
 * Default Archive TTL (30 days)
 */
export const DEFAULT_ARCHIVE_TTL = 2592000000;

/**
 * Default Retention Period (90 days)
 */
export const DEFAULT_RETENTION_PERIOD = 7776000000;

/**
 * Hot Tier TTL (7 days)
 */
export const HOT_TIER_TTL = 604800000;

/**
 * Warm Tier TTL (30 days)
 */
export const WARM_TIER_TTL = 2592000000;
