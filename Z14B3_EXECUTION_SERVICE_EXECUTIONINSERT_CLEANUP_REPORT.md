# Z14B.3 Execution Service ExecutionInsert Cleanup Report

**Phase:** Z14B.3 — EXECUTION SERVICE EXECUTIONINSERT CLEANUP  
**Date:** 2025-05-13  
**Status:** COMPLETE

## Root Cause

The execution.service.ts was injecting persistence-managed timestamp fields (created_at, updated_at) into ExecutionInsert objects, but the canonical ExecutionInsert schema no longer includes these fields as they are now generated by the repository/database layer.

## Exact Fields Changed

### 1. Persistence-Managed Timestamps Removed (2 occurrences)
- **Line 69:** Removed `created_at: new Date().toISOString() as ISODateTime` from createExecution
- **Line 70:** Removed `updated_at: new Date().toISOString() as ISODateTime` from createExecution

## Canonical Schema Reference

ExecutionInsert from types/execution.types.ts:
- Does NOT include `created_at` (now managed by repository/database)
- Does NOT include `updated_at` (now managed by repository/database)
- Includes only business-level fields: tenant_id, agent_name, workflow_type, status, timestamps for lifecycle events (started_at, completed_at, failed_at), retry counts, metadata, etc.

## Service Behavior Preserved
- Execution creation semantics unchanged (timestamps now auto-generated by repository)
- Replay safety preserved (timestamps still tracked by database)
- Tenant isolation unchanged
- Recovery continuity preserved
- Event emission unchanged
- Orchestration integrity unchanged

## File Status

✅ FIXED

## Next First Blocker

**File:** `apps/web/lib/runtime/services/execution.service.ts`  
**Line:** 159  
**Error:**
```
Type error: Object literal may only specify known properties, and 'executionId' does not exist in type '{ table?: string | undefined; column?: string | undefined; constraint?: string | undefined; details?: string | undefined; hint?: string | undefined; context?: Record<string, unknown> | undefined; cause?: Error | undefined; }'.
```

**Context:**
```typescript
{ executionId: id, from: execution.status, to: ExecutionStatus.COMPLETED }
```

**Classification:** TYPE MISMATCH (RuntimeDatabaseError context object schema)

**Reason:** The RuntimeDatabaseError constructor does not accept arbitrary properties like `executionId` in its context object. It only accepts specific fields: table, column, constraint, details, hint, context, cause.

## Report Generated

Z14B3_EXECUTION_SERVICE_EXECUTIONINSERT_CLEANUP_REPORT.md
